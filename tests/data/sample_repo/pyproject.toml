[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "sampledata"
version = "0.1.0"
description = "Sample package for testing smoosh"
requires-python = ">=3.8"
dependencies = [
    "pandas>=2.0.0",
    "pyyaml>=6.0.0",
    "requests>=2.0.0"
]
